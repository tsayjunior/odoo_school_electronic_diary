<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="agenda_escolar.schedules_list">
      <field name="name">agenda_escolar schedules list</field>
      <field name="model">agenda_escolar.schedules</field>
      <field name="arch" type="xml">
        <list>
            <field name="date_start" widget="datetime"/>
            <field name="date_end" widget="datetime"/>
        </list>
      </field>
    </record>

    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="agenda_escolar.schedules_window">
      <field name="name">Materias</field>
      <field name="res_model">agenda_escolar.schedules</field>
      <field name="view_mode">list,form</field>
    </record>


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="agenda_escolar.action_server">
      <field name="name">agenda_escolar server</field>
      <field name="model_id" ref="model_agenda_escolar_agenda_escolar"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->

 
  </data>
</odoo>
