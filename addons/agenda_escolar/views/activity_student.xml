<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="agenda_escolar.activity_student_list">
      <field name="name">agenda_escolar activity_student list</field>
      <field name="model">agenda_escolar.activity_student</field>
      <field name="arch" type="xml">
        <list>
            <field name="view" ></field>
            <field name="note" ></field>
            <field name="state" ></field>
            <field name="activity_id" ></field>
            <field name="estudent_id" ></field>
        </list>
      </field>
    </record>

    <!-- actions opening views on models -->

    <record model="ir.actions.act_window" id="agenda_escolar.activity_student_window">
      <field name="name">Actividad estudiante</field>
      <field name="res_model">agenda_escolar.activity_student</field>
      <field name="view_mode">list,form</field>
    </record>


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="agenda_escolar.action_server">
      <field name="name">agenda_escolar server</field>
      <field name="model_id" ref="model_agenda_escolar_agenda_escolar"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->

 
  </data>
</odoo>
